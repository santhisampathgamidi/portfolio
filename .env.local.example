# Copy to .env.local for local dev. .env.local is gitignored.
# For production (Vercel), set the same env var in the Vercel project settings.

NEXT_PUBLIC_LLM_API_URL=https://santhi-llm-api.onrender.com
